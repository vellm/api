machine:
  node:
    version: 4.5.0

dependencies:
  override:
    - yarn install
  
  post:
    - make fakes3-run
    - make api-serve

test:
  override:
    - tests-dev

deployment:
  aws:
    branch: master
    commands:
      - make api-deploy
