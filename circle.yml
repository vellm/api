machine:
  services:
    - docker

dependencies:
  override:
    - sudo pip install --upgrade docker-compose

test:
  override:
    - make tests-ci

deployment:
  aws:
    branch: master
    commands:
      - make api-deploy
